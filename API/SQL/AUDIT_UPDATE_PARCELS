DELIMITER $$
CREATE TRIGGER AUDIT_UPDATE_PARCELS
AFTER UPDATE
   ON PARCELS FOR EACH ROW
BEGIN
	INSERT INTO `Sda`.`AUDIT` (`UNIQUE_ID`, `COLUMN_NAME`, `OLD_VALUE`, `NEW_VALUE`, `DONE_BY`)	VALUES	(NEW.UNIQUE_ID,	'PARCEL_STATUS', OLD.STATUS, NEW.STATUS, USER());	
END$$ 
DELIMITER ;
